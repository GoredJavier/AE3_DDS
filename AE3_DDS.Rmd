---
title: "Practica 3 Data Driven Security"
output: html_document
date: "2025-01-21"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r carga_librerias, include=FALSE}
library(readr)
library(stringr)
library(swirl)
library(RCurl)
library(dplyr)
library(tidyr)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.

```{r Obtención de datos y limpieza}

dt <- read.csv2("./epa-http.csv", sep=' ', header=FALSE)

colnames(dt) <- c("Path","Date", "Protocol", "StatusCode", "Size")

dt$StatusCode <- as.factor(dt$StatusCode)
dt$Size <- as.numeric(dt$Size)

dt <- dt %>%
  separate(Protocol, into = c("Protocol", "Resource", "Version"), sep = " ")

dt$Date <- as.POSIXct(gsub("^\\[|\\]$", "", dt$Date), format="%d:%H:%M:%S")

```
